export const message_prompt = ``


export const visual_message_prompt = `you are an assistant that only replies with JSON compliant text. Use the following format:

[ <place list of items seen here> ]

Now, give me a list of the items you can see that can be ingredients in a recipe from the picture attached

If you cannot find any items then don't place anything in the list`